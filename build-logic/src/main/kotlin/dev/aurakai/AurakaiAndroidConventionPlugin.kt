package dev.aurakai

import org.gradle.api.Plugin
import org.gradle.api.Project

class AurakaiAndroidConventionPlugin : Plugin<Project> {
    /**
     * Applies this plugin's conventions to the specified Gradle project.
     *
     * Currently a no-op; convention configuration will be added here.
     *
     * @param target The Gradle project to which the plugin is being applied.
     */
    override fun apply(target: Project) {
        // TODO: Add convention logic here
    }
}
